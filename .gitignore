# Terraform local files
.terraform/
*.tfstate
*.tfstate.*
crash.log
crash.*.log

# Variable files often hold personal values (IP, name) - keep your own local
*.auto.tfvars
terraform.tfvars

# Generated SSH private keys - NEVER commit these
*.pem

